<?xml version="1.0" encoding="UTF-8" ?>
<ContentDialog
    x:Class="ClothingRecycler.Desktop.Controls.OutboundOrderConfirmationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ClothingRecycler.Desktop.Models"
    mc:Ignorable="d"
    Width="980"
    CloseButtonText="完成"
    DefaultButton="Close">
    <ScrollViewer
        MaxHeight="760"
        HorizontalScrollBarVisibility="Auto"
        HorizontalScrollMode="Auto">
        <StackPanel MinWidth="940" Spacing="16">
            <StackPanel Spacing="6">
                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="出库确认单" />
                <TextBlock Opacity="0.72" Text="可直接拍照发给客户确认本次出库明细。" TextWrapping="WrapWholeWords" />
            </StackPanel>

            <Border Style="{StaticResource CardSurfaceStyle}">
                <Grid ColumnSpacing="16" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
                        <TextBlock Opacity="0.72" Text="订单号" />
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Confirmation.OrderNumber, Mode=OneWay}" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Spacing="4">
                        <TextBlock Opacity="0.72" Text="交易时间" />
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Confirmation.TimestampText, Mode=OneWay}" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Spacing="4">
                        <TextBlock Opacity="0.72" Text="交易客户" />
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Confirmation.CustomerDisplayName, Mode=OneWay}" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Spacing="4">
                        <TextBlock Opacity="0.72" Text="出库类别" />
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Confirmation.CategoriesText, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <Grid Grid.Row="2" Grid.ColumnSpan="2" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Opacity="0.72" Text="类别数" />
                            <TextBlock Style="{StaticResource DetailMetricValueTextStyle}" Text="{x:Bind Confirmation.CategoryCountText, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Opacity="0.72" Text="数量汇总" />
                            <TextBlock Style="{StaticResource DetailMetricValueTextStyle}" Text="{x:Bind Confirmation.QuantitySummaryText, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Spacing="4">
                            <TextBlock Opacity="0.72" Text="本次总收入" />
                            <TextBlock Style="{StaticResource DetailMetricValueTextStyle}" Text="{x:Bind Confirmation.TotalAmountText, Mode=OneWay}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardSurfaceStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="明细列表" />

                    <Grid ColumnSpacing="12" Padding="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource TableHeaderTextStyle}" Text="交易类别" />
                        <TextBlock Grid.Column="1" Style="{StaticResource TableHeaderTextStyle}" Text="交易量" />
                        <TextBlock Grid.Column="2" Style="{StaticResource TableHeaderTextStyle}" Text="交易单价" />
                        <TextBlock Grid.Column="3" Style="{StaticResource TableHeaderTextStyle}" Text="小计" />
                    </Grid>

                    <ListView
                        ItemsSource="{x:Bind Confirmation.Items, Mode=OneWay}"
                        SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:OrderDetailItemModel">
                                <Grid ColumnSpacing="12" Padding="0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" FontWeight="SemiBold" Text="{x:Bind CategoryName}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind QuantityText}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind UnitPriceText}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3" Text="{x:Bind LineAmountText}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
